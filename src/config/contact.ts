/**
 * Canonical contact information for Stexcore Technology.
 * Single source of truth — keep every contact reference pointing here.
 */

/** Organization display name. */
export const ORG_NAME = "Stexcore Technology";

/** Business phone (United States line). */
export const PHONE = "+1 (213) 396-0066";

/** Phone in international digits-only form, for tel:/wa.me links. */
export const PHONE_DIGITS = "12133960066";

/** WhatsApp deep link. */
export const WHATSAPP_URL = `https://wa.me/${PHONE_DIGITS}`;

/** Portfolio contact email. */
export const EMAIL = "dev@stexcore.net";

/** GitHub organization/profile. */
export const GITHUB_URL = "https://github.com/stexcore";

/** Company website. */
export const SITE_URL = "https://stexcore.net";
